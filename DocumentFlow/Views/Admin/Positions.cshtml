@model IEnumerable<DocumentFlow.Models.Position>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@Html.ActionLink("Добавить должность", "CreatePosition", "Admin")
<br>
<table class="table">
    @if (Model != null && Model.Count() != 0)
    {
        <tr>
            <th>Название</th>
            <th></th>
        </tr>
        foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>
                    @Html.ActionLink("Edit", "EditPosition", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "DeletePosition", new { id = item.Id })
                </td>
            </tr>
        }
    }
    else
    {
        <tr>список пуст</tr>
    }
</table>

